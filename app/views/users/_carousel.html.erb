<% users.each do |user| %>
<div class="tile__small">
        <div class="tile__media">
          <%= link_to cl_image_tag(user.photo.key, class: "tile__img"), user_path(user) %>
          <h3><%= user.username %> </h3>
        </div>
</div>
<% end %>
